<script type="text/javascript">
 $(document).ready(() => {
   $(document)
     .on('tnetc::if-block::add', (e) => {
       process('data-child-calendar', 'timeConditionBlockCalendars', '__child__', true);
     });

   process('data-child-calendar', 'timeConditionBlockCalendars', '__child__', true);
 })
</script>
